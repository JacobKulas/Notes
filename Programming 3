#!/bin/bash

A=$(cat /etc/passwd)
echo $A

A=$(opensll passwd -1 -salt bad4u Password1234)

___________________________________________
If bash script wont run then chmod +777 <scriptname>
___________________________________________

sable or reserved (case-insensitive)#!/bin/bash

contents=$(cat simple2.txt)
if [[ $contents == "tacos" ]]; then
    echo only on tuesdays
elif [[ $contents == "costco" ]]; then 
  echo is cool but expensive
elif [[ $contents == "troopstore" ]]; then
  echo always crowded
else
  echo no tax at commissary
fi

--------------------------------------------------
THIS SORTS BY COLUMN 3 WITH A DELIMITER OF :
cat fakepasswd.txt | sort -t: -k3 -n
ls -l | wc -l <----- command for adding a count to an ls -l
